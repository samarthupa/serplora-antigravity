---
import { getEntry } from 'astro:content';

// 🟢 Define Props
interface Props {
  disableStickyHeader?: boolean;
}

// 🟢 Extract the prop
const { disableStickyHeader = false } = Astro.props;

const headerEntry = await getEntry('header', 'data');
const siteTitle = headerEntry?.data?.siteTitle || 'Serplora';
const logo = headerEntry?.data?.logo;
const logoDark = headerEntry?.data?.logoDark || logo;
const navItems = headerEntry?.data?.navItems || [];

const primaryBtn = {
  label: headerEntry?.data?.primaryButton?.label || 'Sign up for free',
  show: headerEntry?.data?.primaryButton?.show !== false
};
---

<header 
  id="site-header" 
  data-disable-sticky={disableStickyHeader ? "true" : "false"}
  class={`inset-x-0 w-full z-[200] h-[54px] md:h-[58px] bg-main/95 backdrop-blur-[12px] flex items-center justify-center font-['Outfit',sans-serif] transition-transform duration-300 ${disableStickyHeader ? 'relative' : 'sticky top-0'}`}
>
  <div class="w-full px-4 md:px-8 flex items-center gap-3 md:gap-6 relative">
    
    <button 
      type="button" 
      class="md:hidden flex flex-col justify-center items-center w-[28px] h-[36px] rounded-[10px] hover:bg-tx-muted/10 transition-colors focus:outline-none gap-[6px] cursor-pointer shrink-0 border-none p-0 -ml-2 text-tx-main" 
      id="mobile-menu-btn" 
      aria-label="Toggle mobile menu"
    >
      <span class="block w-[22px] h-[1.8px] bg-current rounded-sm transition-transform duration-300 origin-center" id="ham-top"></span>
      <span class="block w-[22px] h-[1.8px] bg-current rounded-sm transition-opacity duration-300" id="ham-mid"></span>
      <span class="block w-[22px] h-[1.8px] bg-current rounded-sm transition-transform duration-300 origin-center" id="ham-bot"></span>
    </button>

    <a href="/" class="flex flex-col items-start justify-center gap-1 shrink-0 outline-none focus-visible:ring-2 focus-visible:ring-tx-muted rounded mr-auto md:mr-0">
      <div class="flex items-center">
        {logo && (
          <>
            <img src={logo} alt={siteTitle} width="160" height="40" class="h-9 md:h-9.5 w-auto object-contain dark:hidden" />
            <img src={logoDark} alt={siteTitle} width="160" height="40" class="h-9 md:h-9.5 w-auto object-contain hidden dark:block" />
          </>
        )}
        {!logo && <span class="text-[18px] md:text-[20px] font-[800] text-tx-main tracking-[-0.5px]">{siteTitle}</span>}
      </div>
    </a>
    
    <ul class="hidden md:flex gap-2 list-none m-0 p-0 relative z-[50]">
      {navItems.map((item, index) => {
        const hasSubItems = item.subItems && item.subItems.length > 0;
        const isLast = index === navItems.length - 1;
        
        return (
          <li class="relative group">
            <a href={item.url || '#'} class="flex items-center px-[10px] py-2 rounded-[8px] text-[15px] font-[550] text-tx-muted transition-all duration-200 hover:bg-main hover:text-tx-main focus-visible:ring-2 focus-visible:ring-tx-muted outline-none">
              {item.label}
              {hasSubItems && (
                <svg class="w-4 h-4 ml-1 opacity-60 group-hover:opacity-100 transition-transform duration-200 group-hover:rotate-180" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M19 9l-7 7-7-7" />
                </svg>
              )}
            </a>

            {hasSubItems && (
              <div class={`absolute ${isLast ? 'right-0' : 'left-0'} top-full pt-1 w-48 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform origin-top translate-y-1 group-hover:translate-y-0 z-[200]`}>
                <div class="py-1 bg-surface rounded-md shadow-sm border border-subtle">
                  {item.subItems.map((subItem: any) => (
                    <a href={subItem.url || '#'} class="block px-4 py-2 text-[14px] text-tx-muted hover:text-tx-main hover:bg-main transition-colors">
                      {subItem.label}
                    </a>
                  ))}
                </div>
              </div>
            )}
          </li>
        );
      })}
    </ul>

    <div class="flex items-center justify-end gap-1.5 md:gap-4 md:ml-auto">
      
      <!-- Search Triggers -->
      <button id="search-trigger" class="flex items-center justify-center w-[40px] h-[40px] rounded-[10px] text-tx-main hover:bg-tx-muted/10 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-tx-muted cursor-pointer shrink-0 md:w-[200px] md:h-[36px] md:px-3 md:bg-tx-muted/5 md:border md:border-subtle/50 md:rounded-[8px] md:text-tx-muted md:hover:bg-tx-muted/10" aria-label="Open Search">
        <svg width="16" height="16" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="shrink-0 md:opacity-70">
          <circle cx="7.5" cy="7.5" r="4.75"></circle>
          <line x1="11" y1="11" x2="16" y2="16"></line>
        </svg>
        <span class="hidden md:block text-[14px] truncate text-left w-full ml-2 opacity-80">Search...</span>
        <span class="hidden md:flex items-center justify-center border border-subtle/60 rounded-[4px] px-1.5 py-0.5 text-[10px] font-mono opacity-60">⌘K</span>
      </button>

      <button class="theme-toggle-btn flex p-2 rounded-full text-tx-muted hover:bg-main hover:text-tx-main transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-tx-muted cursor-pointer" aria-label="Toggle theme">
        <svg class="theme-toggle-dark-icon hidden w-5 h-5 transition-transform duration-300 hover:rotate-12" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path></svg>
        <svg class="theme-toggle-light-icon hidden w-5 h-5 transition-transform duration-300 hover:rotate-90" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path></svg>
      </button>

      <div class="flex items-center">
        
        <div id="auth-signin-header" style="display: none;">
          {primaryBtn.show && (
            <button id="dynamic-signin-btn" type="button" class="ml-2 md:ml-4 flex items-center justify-center h-[34px] md:h-[34px] px-3 md:px-4 rounded-[8px] bg-[#5ecb6b] text-[#0d0e0c] font-['DM_Sans',sans-serif] text-[13px] md:text-[14px] font-[700] tracking-tight transition-all shadow-sm hover:shadow-md hover:bg-[#4eb95a] hover:-translate-y-[1px] active:translate-y-0 cursor-pointer focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-[#5ecb6b] outline-none whitespace-nowrap">
              <span>{primaryBtn.label}</span>
            </button>
          )}
        </div>

        <div id="auth-profile-header" style="display: none;">
          <a href="/account" class="ml-2 md:ml-4 flex items-center justify-center h-[34px] md:h-[36px] px-2.5 md:px-3 rounded-[8px] bg-transparent text-tx-main font-['DM_Sans',sans-serif] text-[13px] md:text-[14px] font-[600] hover:bg-tx-muted/10 transition-colors focus-visible:ring-2 focus-visible:ring-offset-2 outline-none whitespace-nowrap">
            <img id="profile-avatar-img-header" src="" alt="Profile" class="w-6 h-6 rounded-full object-cover shrink-0 hidden" />
            <div id="profile-avatar-header" class="w-7 h-7 rounded-full bg-[#5ecb6b] text-[#0d0e0c] flex items-center justify-center font-extrabold text-[12px] shrink-0 hidden"></div>
            <span class="hidden md:inline ml-2.5">Profile</span>
          </a>
        </div>
        
      </div>
    </div>

    <div id="mobile-menu" class="hidden absolute top-[54px] left-0 w-full bg-main shadow-xl flex-col h-[calc(100dvh-54px)] overflow-y-auto z-[190]">
      <div class="flex flex-col h-full px-6 py-6 w-full max-w-[500px] mx-auto">
        <nav class="flex flex-col gap-2 mb-8 mt-4">
          {navItems.map((item: any) => {
             const hasSubItems = item.subItems && item.subItems.length > 0;
             return (
               <div class="border-b border-subtle group">
                 <div class="flex items-center justify-between">
                   <a href={item.url || '#'} class="text-lg font-[800] tracking-tight text-tx-main hover:opacity-80 transition-opacity flex-1 leading-none py-2">{item.label}</a>
                   {hasSubItems ? (
                     <button class="mobile-submenu-toggle p-2 text-tx-muted focus:outline-none" aria-expanded="false" aria-label="Toggle submenu">
                       <svg class="w-6 h-6 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" /></svg>
                     </button>
                   ) : (
                     <a href={item.url || '#'} class="p-2 text-tx-muted opacity-40 group-hover:opacity-100 transition-opacity" aria-label={`Go to ${item.label}`}>
                       <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 17L17 7M7 7h10v10" /></svg>
                     </a>
                   )}
                 </div>
                 {hasSubItems && (
                   <div class="mobile-submenu hidden flex-col gap-4 pb-6 pt-2 pl-2">
                     {item.subItems.map((subItem: any) => (
                       <a href={subItem.url || '#'} class="text-[18px] font-[500] text-tx-muted hover:text-tx-main transition-colors flex items-center gap-2">
                         <span class="w-1.5 h-1.5 rounded-full bg-blue-500/50"></span>
                         {subItem.label}
                       </a>
                     ))}
                   </div>
                 )}
               </div>
             );
          })}
        </nav>
      </div>
    </div>
  </div>
</header>

<!-- 🟢 MINIMALIST SEARCH OVERLAY -->
<div id="minimal-search-overlay" class="fixed inset-0 z-[300] bg-surface/95 backdrop-blur-md opacity-0 invisible pointer-events-none transition-all duration-300 flex flex-col pt-[12vh] px-5">
  <div class="absolute inset-0" id="search-overlay-backdrop"></div>

  <div
    id="search-overlay-content"
    class="relative z-10 w-full max-w-2xl mx-auto transform scale-95 opacity-0 transition-all duration-500 ease-out"
  >
    <!-- Close Button -->
    <div class="flex justify-end mb-5">
      <button
        type="button"
        id="close-search-overlay"
        class="text-s tracking-[0.2em] text-tx-muted hover:text-tx-main transition-colors outline-none"
        aria-label="Close"
      >
        Close
      </button>
    </div>

    <!-- Search Form -->
    <form action="/search" method="GET" class="w-full">
      <div class="border-b border-tx-muted/20 pb-3 focus-within:border-tx-main transition-colors">
        <input
          type="search"
          name="q"
          id="overlay-search-input"
          placeholder="Search..."
          autocomplete="off"
          spellcheck="true"
          autocorrect="on"
          autocapitalize="off"
          class="w-full bg-transparent border-none outline-none appearance-none p-0 text-2xl md:text-3xl font-semibold tracking-tight text-tx-main placeholder:text-tx-muted/40"
        />
      </div>

      <!-- Search Button (Hidden by default) -->
      <div id="search-submit-container" class="mt-6 flex justify-end opacity-0 invisible transition-all duration-200">
        <button
          type="submit"
          class="inline-flex items-center gap-2 rounded-full border border-subtle px-4 py-2 text-sm font-medium text-tx-main transition-all duration-200 hover:border-tx-main hover:bg-surface-secondary focus:outline-none focus:ring-2 focus:ring-tx-main/20"
        >
          <svg
            class="h-4 w-4"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"
            />
          </svg>
          <span>Search</span>
        </button>
      </div>
    </form>
  </div>
</div>

<script>
  // Theme Toggle Logic
  const themeToggleBtns = document.querySelectorAll('.theme-toggle-btn');
  const darkIcons = document.querySelectorAll('.theme-toggle-dark-icon');
  const lightIcons = document.querySelectorAll('.theme-toggle-light-icon');

  if (themeToggleBtns.length > 0) {
    if (document.documentElement.classList.contains('dark')) {
      lightIcons.forEach(icon => icon.classList.remove('hidden'));
    } else {
      darkIcons.forEach(icon => icon.classList.remove('hidden'));
    }

    themeToggleBtns.forEach(btn => {
      btn.addEventListener('click', function() {
        const isCurrentlyDark = document.documentElement.classList.contains('dark');
        
        darkIcons.forEach(icon => icon.classList.toggle('hidden'));
        lightIcons.forEach(icon => icon.classList.toggle('hidden'));

        if (isCurrentlyDark) {
          document.documentElement.classList.remove('dark');
          localStorage.setItem('theme', 'light');
        } else {
          document.documentElement.classList.add('dark');
          localStorage.setItem('theme', 'dark');
        }
      });
    });
  }

  // --- MINIMAL SEARCH OVERLAY LOGIC ---
  const searchTrigger = document.getElementById('search-trigger');
  const searchOverlay = document.getElementById('minimal-search-overlay');
  const searchBackdrop = document.getElementById('search-overlay-backdrop');
  const searchContent = document.getElementById('search-overlay-content');
  const searchInput = document.getElementById('overlay-search-input') as HTMLInputElement;
  const closeSearchBtn = document.getElementById('close-search-overlay');
  const searchSubmitContainer = document.getElementById('search-submit-container');

  function openSearch() {
    searchOverlay?.classList.remove('opacity-0', 'invisible', 'pointer-events-none');
    searchContent?.classList.remove('scale-95', 'opacity-0');
    searchContent?.classList.add('scale-100', 'opacity-100');
    document.body.style.overflow = 'hidden';
    
    setTimeout(() => {
      searchInput?.focus();
    }, 100);
  }

  function closeSearch() {
    searchOverlay?.classList.add('opacity-0', 'invisible', 'pointer-events-none');
    searchContent?.classList.remove('scale-100', 'opacity-100');
    searchContent?.classList.add('scale-95', 'opacity-0');
    document.body.style.overflow = '';
    
    // Clear input and hide button after closing animation finishes
    setTimeout(() => {
      if (searchInput) searchInput.value = '';
      searchSubmitContainer?.classList.add('opacity-0', 'invisible');
    }, 300);
  }

  // Toggle button visibility based on input text
  searchInput?.addEventListener('input', () => {
    if (searchInput.value.trim().length > 0) {
      searchSubmitContainer?.classList.remove('opacity-0', 'invisible');
    } else {
      searchSubmitContainer?.classList.add('opacity-0', 'invisible');
    }
  });

  searchTrigger?.addEventListener('click', openSearch);
  searchBackdrop?.addEventListener('click', closeSearch);
  closeSearchBtn?.addEventListener('click', closeSearch);

  // Handle Cmd+K / Esc keyboard shortcuts
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeSearch();
    if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
      e.preventDefault();
      openSearch();
    }
  });

  // Mobile Menu Logic
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const topBar = document.getElementById('ham-top');
  const midBar = document.getElementById('ham-mid');
  const botBar = document.getElementById('ham-bot');

  if (btn && menu && topBar && midBar && botBar) {
    btn.addEventListener('click', () => {
      menu.classList.toggle('hidden');
      menu.classList.toggle('flex');
      
      if (menu.classList.contains('hidden')) {
        document.body.style.overflow = '';
        topBar.style.transform = '';
        midBar.style.opacity = '1';
        botBar.style.transform = '';
      } else {
        document.body.style.overflow = 'hidden';
        topBar.style.transform = 'translateY(7px) rotate(45deg)';
        midBar.style.opacity = '0';
        botBar.style.transform = 'translateY(-7px) rotate(-45deg)';
      }
    });
  }

  const submenuToggles = document.querySelectorAll('.mobile-submenu-toggle');
  submenuToggles.forEach(toggle => {
    toggle.addEventListener('click', (e) => {
      e.preventDefault();
      const svg = toggle.querySelector('svg');
      const submenu = toggle.closest('div.group')?.querySelector('.mobile-submenu');

      if (submenu && svg) {
        submenu.classList.toggle('hidden');
        submenu.classList.toggle('flex');
        if (submenu.classList.contains('hidden')) {
          svg.style.transform = '';
          toggle.setAttribute('aria-expanded', 'false');
        } else {
          svg.style.transform = 'rotate(180deg)';
          toggle.setAttribute('aria-expanded', 'true');
        }
      }
    });
  });

  // Smart header logic
  const header = document.getElementById('site-header');
  const isStickyDisabled = header?.getAttribute('data-disable-sticky') === 'true';
  let lastScrollY = window.scrollY;

  window.addEventListener('scroll', () => {
    if (!header || isStickyDisabled) return;
    
    const currentScrollY = window.scrollY;
    
    if (Math.abs(currentScrollY - lastScrollY) < 5) return;

    const isMenuClosed = !document.getElementById('mobile-menu')?.classList.contains('flex');
    const isSearchClosed = searchOverlay?.classList.contains('invisible');
    
    if (currentScrollY > 48 && isMenuClosed && isSearchClosed) {
      if (currentScrollY > lastScrollY) {
        header.classList.add('-translate-y-full');
        document.documentElement.classList.add('header-hidden'); 
      } else {
        header.classList.remove('-translate-y-full');
        document.documentElement.classList.remove('header-hidden'); 
      }
    } else if (currentScrollY <= 48) {
      header.classList.remove('-translate-y-full');
      document.documentElement.classList.remove('header-hidden');
    }
    
    lastScrollY = currentScrollY;
  }, { passive: true });

  // --- REVISED CLIENT-SIDE AUTHENTICATION LOGIC ---
  import { authClient } from '../../lib/auth-client';

  const signinHeader = document.getElementById('auth-signin-header');
  const profileHeader = document.getElementById('auth-profile-header');
  const avatarImgEl = document.getElementById('profile-avatar-img-header') as HTMLImageElement;
  const avatarFallbackEl = document.getElementById('profile-avatar-header');

  async function checkAuthSession() {
    try {
      const { data } = await authClient.getSession();
      
      if (data && data.user) {
        const user = data.user as any;

        if (user.image && avatarImgEl) {
           avatarImgEl.src = user.image;
           avatarImgEl.classList.remove('hidden');
        } else if (avatarFallbackEl && user.name) {
           avatarFallbackEl.textContent = user.name.charAt(0).toUpperCase();
           avatarFallbackEl.classList.remove('hidden');
        }

        if (signinHeader) signinHeader.style.display = 'none';
        if (profileHeader) profileHeader.style.display = 'flex';
      } else {
        if (signinHeader) signinHeader.style.display = 'block';
        if (profileHeader) profileHeader.style.display = 'none';
      }
    } catch (error) {
      if (signinHeader) signinHeader.style.display = 'block';
      if (profileHeader) profileHeader.style.display = 'none';
    }
  }

  checkAuthSession();

  // --- DYNAMIC SIGN-IN ROUTING ---
  const dynamicSigninBtn = document.getElementById('dynamic-signin-btn');
  if (dynamicSigninBtn) {
    dynamicSigninBtn.addEventListener('click', () => {
      const currentPath = window.location.pathname + window.location.search;
      window.location.href = `/login?returnUrl=${encodeURIComponent(currentPath)}`;
    });
  }
</script>